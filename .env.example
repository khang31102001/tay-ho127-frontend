# Base URL của Backend ASP.NET Core Web API. Để trống khi backend chưa sẵn
# sàng — src/lib/http/api-client.ts sẽ gọi tương đối trên chính origin hiện
# tại. Khi backend thật sẵn sàng, set giá trị này (vd. https://api.tayho127.vn)
# và chạy `cp .env.example .env.local` — không cần sửa code.
NEXT_PUBLIC_API_URL=
